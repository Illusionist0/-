//成绩
#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main(){
    double a, b, c;
    cin >> a >> b >> c;
    cout << (int)(a * 0.2 + b * 0.3 + c * 0.5);
    system("pause");
    return 0;
}